# Dépendances du pipeline planifié serverless (GitHub Actions cron).
# Équivalent des deps Airflow : ETL + monitoring (Evidently) + entraînement.
# evidently épinglé en 0.4.x (API utilisée par monitoring/monitor.py).
pandas>=2.0
boto3>=1.34
requests>=2.31
python-dotenv>=1.0
sqlalchemy>=1.4
psycopg2-binary>=2.9
mlflow>=2.12,<3.0
scikit-learn==1.7.2
evidently>=0.4.11,<0.5
kaggle>=1.6
